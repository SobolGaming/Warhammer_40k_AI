# Superseded diagnostic driver

These observations used an early driver that stopped at the shot marker before
requiring attack completion. Retain numeric diagnostics, but do not use their
completion_rate fields as certification. The parent directory contains the
corrected, explicitly completed base/head comparison.
